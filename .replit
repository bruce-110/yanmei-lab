# Replit configuration for YANMEI LAB

entrypoint = "qwen_main.py"

[deployment]
run = "streamlit run qwen_main.py --server.headless true --server.port 8501"
deploymentTarget = "autoscale"

[autoscale]
enabled = true
